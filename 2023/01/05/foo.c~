#include <stdio.h>
#include <stdlib.h>

void inutil()
{
    puts("Como foi que cheguei aqui?");
    exit(1);
}

void util()
{
    void (*nada[0])();
    1[nada] = inutil;
}

int main(int argc, char **argv)
{
    util();
    return 0;
}
