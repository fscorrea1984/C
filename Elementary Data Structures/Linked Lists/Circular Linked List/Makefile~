BIN = doublylinkedlist

OBJ = doublylinkedlist.o

SRC = doublylinkedlist.c

FLAGS = -g -glldb -w -std=c11

CC = clang

all: doublylinkedlist.c
	$(CC) $(FLAGS) $(SRC) -o $(BIN)
clean:
	rm -f $(BIN) $(OBJ) *~
	clear
